/* @flow weak */
'use strict'

const TemplateBundle  = require('./template-bundle');
const BasicTemplate   = require('./basic-template');
const PollingTemplate = require('./polling-template');

module.exports = {
  TemplateBundle : TemplateBundle,
  BasicTemplate  : BasicTemplate,
  PollingTemplate: PollingTemplate
};
